rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated users (signed in via this app's Firebase Auth) can read/write.
    // Cloud Functions using the Admin SDK are not subject to these rules.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
